VERSIONE=v04
NOTE=Aggiunto avviso se il file non è protetto da password di apertura; ripristinato il limite di rotazione backup a 10 copie; l'aggiornamento ora verifica un CHECKSUM del file scaricato e non usa più curl.exe (funziona anche su Linux/macOS); l'aggiornamento ora sovrascrive direttamente il file in uso (il file precedente resta comunque salvato in Backup); corretto l'ordine di tabulazione (tasto Tab) nella scheda anagrafica del dialogo iscritti; corretto il calcolo delle scadenze in modo che un documento "in scadenza" non venga più nascosto quando un altro documento dello stesso iscritto risulta scaduto, sia nei contatori sia nell'avviso all'apertura del file; le liste "in scadenza"/"scaduti" ora mostrano la data di scadenza effettiva (rilascio + 1 anno) invece della sola data di rilascio; corretto un bug per cui, se un aggiornamento falliva, il messaggio "Dettaglio:" risultava sempre vuoto invece di spiegare la causa reale; corretto un bug per cui la sostituzione finale del file in uso falliva con errore "AlreadyExists" quando il file era ancora aperto (accadeva sempre, nell'uso reale): ora la sostituzione avviene con una copia di basso livello che non richiede di chiudere prima il file; corretto un bug per cui il controllo del CHECKSUM del file scaricato risultava sempre "non corrispondente" anche quando il valore atteso e quello ottenuto, mostrati entrambi a video, erano identici: il confronto ora avviene tra le due stringhe esadecimali invece che tra due numeri convertiti internamente, evitando un comportamento inaffidabile riscontrato con valori il cui bit più alto è impostato; corretto un bug per cui la sostituzione finale del file falliva con l'errore "a file or directory could not be deleted": il codice cancellava inutilmente il file originale prima di sostituirlo, mentre quel file era ancora aperto (il gestionale stesso, in esecuzione); ora la sostituzione avviene con una copia diretta che sovrascrive da sola il file esistente, senza bisogno di cancellarlo prima; corretto un bug per cui, dopo un aggiornamento completato con successo, la chiusura automatica del file falliva con l'errore "CloseVetoException" (il file restava aperto, sovrascritto ma non richiuso): un primo tentativo di correzione si è rivelato inefficace (l'errore si ripresentava identico), quindi è stata adottata una gestione errori esplicita che intercetta sempre l'eventuale rifiuto, riprova una seconda volta dopo una breve pausa, e in ultima analisi rinuncia alla sola chiusura automatica senza generare mai più un errore visibile: il file, già salvato e aggiornato, resta al più aperto e va chiuso manualmente. Nessun cambiamento nella struttura dati o nelle funzioni già testate.
URL=https://github.com/giusepp3-dotcom/DUSTrgs/releases/download/v04/RegistroSoci_ASD_v04.ods
CHECKSUM=2F9FFBAC
